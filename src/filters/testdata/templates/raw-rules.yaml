title: Template with raw rules
params:
  - name: boolean_param
    description: A boolean parameter
    type: checkbox
    default: false
    rules: |
      helloA
      helloB
  - name: another_boolean
    description: A disabled boolean parameter
    type: checkbox
    default: false
    rules: "helloC" # Intentional "normal quotes" to make sure we don't mess up newlines
tags: ["tag1", "tag2"]
---

## Test description title
