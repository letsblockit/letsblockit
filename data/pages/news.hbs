{{#each releases}}
    <div class="card mb-3">
        <div class="card-header {{#if (lookup @root.data.newReleases @index)}}bg-success{{/if}}">
            {{date}}
        </div>
        <div class="card-body">
                {{{Description}}}
        </div>
    </div>
{{/each}}